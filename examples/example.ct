<% use "example.yaml" %>
#include <stdio.h>

<% example[0].const %>

int main(void)
{
	const <% example[2].type %> v = 0;

	printf("Hi there, <%%>%> <% example[1].format %> <%%>%>!\n", v);
	return 0;
}
